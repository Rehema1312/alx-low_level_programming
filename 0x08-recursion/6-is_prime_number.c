#include "main.h"
/**
 * is_prime_number - function returning 1 if the integer is a prime number otherwise return 0
 * @n: parameter to be tested
 * Return: always return Success
 */
int is_prime_number(int n)
{
	if ( n / 2 )
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
