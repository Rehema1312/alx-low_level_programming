#include <stdio.h>
/**
 * main-putchar function
 * Return- always return 0 (success)
 */
int main(void)
{
		int ch;

		for (ch = 'a'; ch < = 'z'; ch++)
			putchar(ch);
		putchar ('\n');
		/*print lowercase letters*/
		get ch();
		return (0);
}
