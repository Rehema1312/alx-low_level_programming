#include <stdio.h>
/**
 * main-putchar function
 * Return- always return0 (success)
 */
main(void)
{
		int ch;

		for (ch = 'a'; ch < = 'z'; ch++)
			putchar(ch);
		putchar ('\n');
		/*print lowercase letters*/
		getch();
}
